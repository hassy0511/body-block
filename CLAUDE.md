# からだブロック凹凸 うつしてポン！ — プロジェクト仕様書

Claude Code に開発を依頼するためのマスタードキュメント。
このファイルをリポジトリの `CLAUDE.md` またはプロジェクト直下の仕様書として配置する。

---

## 1. コンセプト

カメラで撮影した人の姿を切り抜き、その形を「ブロック」としてゲーム内で遊ぶ、
**家庭向けミニゲーム集**。スマホ・タブレットのブラウザで動作する。

- ターゲット: 家庭（親子・兄弟・家族会）。子どもが中心だが大人も一緒に遊べる
- 体験の核: **複数人で「せーの！」と同時撮影する瞬間の盛り上がり**
- 全モード **1人でも複数人でも遊べる** ことを原則とする
- ゆくゆくは販売も視野。初期から訴訟リスク・プライバシーリスクを避ける設計とする

## 2. 技術構成

| 要素           | 採用技術                                | 備考                                                           |
| -------------- | --------------------------------------- | -------------------------------------------------------------- |
| 配信           | GitHub Pages（静的サイト）              | HTTPS 必須（カメラ利用のため）。ビルド成果物を gh-pages に配置 |
| フレームワーク | Phaser 3 + TypeScript + Vite            | 「名産クエスト」と同構成。流用可                               |
| 物理演算       | Matter.js（Phaser 3 内蔵）              |                                                                |
| カメラ         | `getUserMedia`                          | 静止画スナップショット方式（リアルタイム追跡はしない）         |
| 人物切り抜き   | MediaPipe Image Segmenter（WASM + GPU） | Apache 2.0。商用利用可                                         |
| 保存           | localStorage（選択制）                  | 後述の保存ポリシー参照                                         |

### 2.1 処理は全てクライアント完結（重要）

画像・切り抜き結果を**一切サーバーへ送信しない**。
子どもの姿を扱うため、これはプロダクトの強みであり将来のストア審査・
プライバシーポリシーの根拠になる。外部送信するコードを絶対に書かないこと。

### 2.2 切り抜きパイプライン（共通基盤の中核）

```
撮影ボタン → 静止画1枚キャプチャ
  → MediaPipe Image Segmenter で人物マスク（二値）取得
  → マスクを縮小（例: 幅256px）してノイズ処理
      - モルフォロジー処理（膨張・収縮）で輪郭を平滑化
      - 面積閾値で小さいノイズ塊を除去
  → 連結成分ラベリング（flood fill）で塊ごとに分割 = 人ごとの分離
  → 塊ごとに:
      - アルファ付きスプライト生成（offscreen canvas 合成）
        ※輪郭に白フチを付けてステッカー風にする（切り抜きの粗を隠す効果も兼ねる）
      - マーチングスクエアで輪郭抽出
      - Douglas-Peucker で頂点間引き
      - poly-decomp で凸分割
      - Matter.Bodies.fromVertices で剛体化
  → { sprite, body, area, centroidX } の配列としてゲームシーンへ渡す
```

- 人同士が重なって写った場合は 1 塊になる（仕様として許容）
- 検出塊数が指定プレイヤー数と一致しない場合は「離れて撮り直してね」とリトライ誘導
- プレイヤーへの所有権割り当ては **重心の X 座標**（左にいた人 = P1）で行い、
  撮影プレビューの立ち位置ガイド枠と整合させる

### 2.3 最初に行う技術検証（ステップ1 に含める）

実装より先に、以下を実機（スマホ / タブレット）で検証し結果を記録すること:

1. **モデル比較**: Selfie Segmentation（近距離向け）vs Multiclass Selfie Segmenter /
   DeepLab 系。**全身・2〜3m 距離・複数人**の構図でどちらが実用的か
2. **家庭背景での精度**: ごちゃごちゃしたリビング背景で人だけ抜けるか。
   誤検出（家具・ぬいぐるみ・ポスター等）の頻度と、ノイズ処理での改善度
3. **複数人分離**: 離れて立った 2 人が別塊としてラベリングできるか
4. **処理時間**: 撮影→スプライト生成完了までの実測（目標: 体感一瞬、〜1秒程度）

### 2.4 撮影 UX（共通）

- 撮影プレビュー画面で**セグメンテーションマスクをリアルタイム表示**し、
  「ちゃんと抜けているか」をユーザー自身が確認してから撮れるようにする
- プレイヤー人数分の**立ち位置ガイド枠**を表示
- カウントダウン（3・2・1）→ シャッター。「せーの！」の一体感を演出する

## 3. 収録モード一覧（開発順）

| #   | モード名             | 種別           | 物理演算           | 概要                                                                             |
| --- | -------------------- | -------------- | ------------------ | -------------------------------------------------------------------------------- |
| 1   | ポーズでハマる！     | 型はめパズル   | 不要               | 画面のシルエット穴に合うポーズで撮影。マスク重なり率でスコア化                   |
| 2   | たいそうタワー       | 協力・積み上げ | 必要               | 全員の体ブロックが降ってきて積み上がる。目標ライン(高さ選択制)まで積めたらクリア |
| 3   | まもって！シェルター | 協力・防御     | 必要               | 降ってくるボールから体ブロックを配置して小キャラを守る                           |
| 4   | こわしてバトル       | 対戦・崩し     | 必要               | 交互に体ブロックを落とし、先に相手陣地のタワーを崩した方が勝ち                   |
| 5   | いきものずかん       | 鑑賞           | 軽微（浮遊・衝突） | 切り抜いた体が生き物として水槽/草原ステージをふよふよ動く。低年齢向け鑑賞モード  |
| 6   | コロコロゴール！     | 協力・パズル   | 必要               | 体ブロックで「みち」を作り、転がるボールをゴールのかごまで導く                   |
| 7   | あめあめキャッチ     | 順番に対戦     | 必要               | おわんポーズで降ってくるアメを受け止め、乗った数を競う                           |
| 8   | なりきりパネル       | 型はめ・記念   | 不要               | 顔ハメパネルの全身版。プリセット背景の穴にハマって合成写真を作る（素材待ち）     |

- 1〜7 は実装ずみ。8 は背景素材の発注待ち（要件は `SPEC_MODE8_PANEL.md` §4）
- 詳細仕様: モード1 `SPEC_MODE1.md` / モード2 `SPEC_MODE2.md` / モード3 `SPEC_MODE3.md`
  / モード4 `SPEC_MODE4.md` / モード5 `SPEC_MODE5.md` / モード6 `SPEC_MODE6.md`
  / モード7 `SPEC_MODE7.md` / モード8 `SPEC_MODE8_PANEL.md`
- 動作確認チェックリスト: 配信サイトの `check.html`（`public/check.html`）

## 4. 保存ポリシー（選択制・デフォルト保存なし）

- 撮影した切り抜きは**使い捨てが基本**。セッション終了で消える
- 撮影後に「とっておく？」の選択肢を出し、ユーザーが選んだものだけ
  localStorage に保存する（オプトイン）
- 保存済み切り抜きの一覧・削除機能を設定画面に用意する
- この「デフォルトで残さない」設計は変更しないこと（プライバシー方針の根幹）

## 5. 法的リスク回避方針

- ゲームの仕組み・ルール自体は問題ない。ただし以下を厳守:
  - **「トンデミ」「バンダイナムコ」等の商標名・ロゴへの言及を、コード・UI・
    紹介文・README のいずれにも一切入れない**（「〜にインスパイア」も書かない）
  - 特定アトラクションの画面デザイン・キャラクター・演出を模倣しない
  - 使用ライブラリのライセンス表記（MediaPipe: Apache 2.0 等）を
    クレジット画面に記載する
- 効果音・BGM・フォント・画像素材は、商用利用可のライセンスが明確なもの
  または自作のみを使う。出所をアセット管理表に記録する

## 6. 開発上の共通ルール

- 対象ブラウザ: iOS Safari / Android Chrome を最優先。次点で PC Chrome
- 画面は縦横両対応だが、**縦向き（720x1280）を基準**にデザインする
  - 上から下へ落ちるゲームは縦の余白が命で、縦向きのほうが遊びとして成立する
  - 一度「大画面＝横長」を理由に横向き基準へ変えたが、
    **iPad の横持ちで実測したところ縦持ちスマホと大差なく、縦向きへ戻した**。
    横長の画面（テレビ等）につないだ場合は左右に余白が出るが許容する

### 遊ぶ環境の前提

**全身を写すには端末から離れる必要があり、スマホ単体では画面が読めない。**
これは設計の工夫では解決できない（画角の物理的な制約）。

| 撮る人     | 必要な距離 |
| ---------- | ---------- |
| 身長 1.2m  | 約 1.5m    |
| 身長 1.7m  | 約 2.2m    |
| 2 人ならぶ | 3m 前後    |

そのため、以下を推奨環境として案内する。

- **推奨: テレビ・大型モニターにつないで遊ぶ**（ミラーリングでよい）
- **次点: タブレット**
- スマホ単体は「動くことは動く」程度に考える

なお超広角カメラを使えば必要な距離を 1m 弱まで縮められるが、
超広角は背面カメラにしかなく画面がこちらを向かない。
また機種にも依存するため、前提にはしない。

- 文言は全てひらがな中心（対象年齢を考慮）。漢字を使う場合はふりがな相当の配慮
- UI トーンは「ぼうけんドリル」「名産クエスト」と並ぶ親しみやすいシリーズ感
- **ホーム画面追加でのアプリ表示（standalone）は導入済み**。
  マニフェスト + meta タグのみで実現しており Service Worker は使っていない
- Service Worker（オフラインキャッシュ）は初期スコープ外
  （ぼうけんドリルでの問題事例を踏まえ、安定後に検討）。
  standalone 表示とは別物なので混同しないこと

## 7. リポジトリ構成（想定）

```
/
├─ CLAUDE.md            … 本仕様書
├─ SPEC_MODE1.md        … モード1 詳細仕様
├─ src/
│  ├─ core/            … 共通基盤
│  │  ├─ camera.ts     … getUserMedia ラッパ / プレビュー
│  │  ├─ segmenter.ts  … MediaPipe 呼び出し
│  │  ├─ mask.ts       … ノイズ処理・ラベリング・輪郭抽出
│  │  ├─ cutout.ts     … スプライト生成 / ポリゴン化 / 剛体化
│  │  └─ storage.ts    … 選択制保存
│  ├─ scenes/          … Phaser シーン（タイトル / 撮影 / 各モード）
│  └─ main.ts
├─ public/              … 静的アセット
└─ vite.config.ts       … GitHub Pages 用 base 設定
```
