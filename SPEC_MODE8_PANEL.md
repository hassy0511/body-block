# モード8「なりきりパネル」詳細仕様 + 背景素材の発注要件

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
観光地にある「顔ハメパネル」の**全身版**。ポーズでハマる！(モード1)の応用。

**状態: 背景素材待ち。**素材の生成は外部(Codex 等)へ発注する。
§4 がそのまま発注書になるように書いてある。

---

## 1. ゲーム概要

かわいい/かっこいいシチュエーションが描かれたパネルに、
人型の「穴」が空いている。穴にピッタリはまるポーズで撮影すると、
自分がその場面の主人公になった1枚ができあがる。

- 判定はモード1と同じ `judgePose()`（√IoU スコア）。ハマり度で盛り上がる
- **ごほうびは合成写真**。判定後、撮った切り抜きをパネルの穴に
  はめ込んだ完成絵を大きく表示する（ここがモード1との違い。
  点数より「なりきれた絵」が主役）
- 保存はいつも通り選択制（CLAUDE.md §4。デフォルトでは残さない）

## 2. 画面フロー

モード1の流れを流用する。

```
タイトル → なりきりパネル → パネルえらび(10種の一覧)
  → 撮影画面(パネルを重ね表示。穴の中は透明・外はパネルの絵)
  → 判定 → 完成絵の表示(ハマり度つき) → もういちど / べつのパネル
```

- モード1と違い、穴の外は「暗い壁」ではなく**パネルの絵そのもの**を重ねる
- お手本シルエット・リアルタイムのハマり度表示はモード1と同じ

## 3. 実装メモ

- お題データはモード1のテーマ(`src/themes/`)と同じ仕組みに載せる。
  判定用マスクは §4 のマスク画像から読み込み時に生成(幅256px縮小・二値化)
- 完成絵 = パネル画像を描いた上に、撮影した切り抜きスプライトを
  穴の位置へ重心合わせで重ねる(位置合わせは `alignMask` と同じ変換)
- ポーズが物理的に取れることは素材側の責任(§4 の必須条件)。
  モード1で「機械の描いた形は取れない」問題が起きたことへの対策

## 4. 背景素材の発注要件（このまま発注に使う）

### 納品物

10テーマ。各テーマにつき PNG を2枚、計20枚。

| ファイル名            | 内容                                                                     |
| --------------------- | ------------------------------------------------------------------------ |
| `panel_<id>.png`      | カラーのパネル絵。1280x720。**人が入る穴の部分だけ完全透明**(アルファ0)  |
| `panel_<id>_mask.png` | 判定用マスク。1280x720。**白 = 穴、黒 = それ以外**の完全二値(中間色なし) |

- 2枚の穴の形は**ピクセル単位で一致**させること(同じ形状データから書き出す)
- 絵の中に文字・ロゴを入れない(UI 側で名前を出す)

### 穴(ポーズ)の必須条件

- **立った人が実際に取れるポーズだけ**にする。
  寝る・座る・浮く・またがる等、床に立ったままできない形は不可
  (プレイヤーはカメラの前に立って撮るため)
- 全身シルエット(頭〜足)。足元は画像の下端から 5〜10% の高さに揃える
- 穴の幅は画像幅の 20〜40%、位置は中央 60% の範囲内
- 幅 256px に縮小しても腕・脚が判別できる、太めで単純な形にする
  (細い指先・ひらひらした装飾は潰れるので穴に含めない)

### 絵柄

- 家庭・子ども向けのフラットで温かい絵本調。
  ゲームの基調色(クリーム #fff3e0 / オレンジ #ff8a3d / 青緑 #33bfc7)と
  なじむ彩度
- 実在の作品・キャラクター・ロゴ・施設を連想させる意匠は不可
  (CLAUDE.md §5。オリジナルであること)
- 商用利用可能であること(生成物の権利関係を確認して納品する)

### テーマ 10 種

| id          | 場面                                   | 穴のポーズ(立ってできる形)           |
| ----------- | -------------------------------------- | ------------------------------------ |
| `hero`      | ドラゴンと向き合う勇者(オリジナルの竜) | 剣を高く掲げる(片腕上げ)             |
| `astronaut` | 月面と地球                             | 両腕を大きく上げてバンザイ           |
| `ninja`     | 夜の屋根の上                           | 忍者の構え(片腕前・片腕後ろ)         |
| `royal`     | お城のバルコニー                       | 手を振る(片腕上げ・すまし立ち)       |
| `pirate`    | 海賊船の甲板と宝箱                     | 腰に手・片腕で遠くを指す             |
| `dino`      | 大きな恐竜(オリジナル)と並ぶ           | 恐竜に手を振る(両腕横に広げ)         |
| `circus`    | サーカスの綱渡り(綱は床の高さに描く)   | 両腕を水平に広げてバランス           |
| `chef`      | 巨大ケーキとお菓子の家                 | 両腕でケーキを差し出すように前へ     |
| `robo`      | ロボット工場                           | カクカクしたロボットポーズ(ひじ直角) |
| `flower`    | 花畑と虹                               | 大の字                               |

## 5. スコープ外

- パネルの追加購入・ダウンロード
- 2人用パネル(まずは1人用10種。手応えを見て拡張)
- 完成絵の自動保存(選択制保存の仕組みに載せる)
