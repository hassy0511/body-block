# 技術検証メモ

SPEC_MODE1.md §6 / CLAUDE.md §2.3 の検証結果を記録する。
検証ページ: `verify.html`(公開URL の `/body-block/verify.html`)

---

## 検証ページの使い方

1. 公開URL(HTTPS)または `npm run dev` のローカルURLを実機で開く
2. カメラ許可を出す → プレビューにマスクが水色で重畳表示される
3. 画面上部のセレクトでモデルを切り替える(切り替えのたびに再読み込みされる)
4. 「静止画キャプチャ」で切り抜き結果と処理時間がログに出る

ログには以下が出力される:

- モデル読み込み時間 / 使用デリゲート(GPU or CPU)
- キャプチャ解像度 / 人物ピクセルの割合 / 撮影→切り抜き表示までの処理時間

---

## 1. モデル比較(全身 2〜3m・複数人構図)

| 観点                     | Selfie Segmentation (landscape) | Multiclass Selfie Segmenter |
| ------------------------ | ------------------------------- | --------------------------- |
| モデル読み込み時間       |                                 |                             |
| デリゲート(GPU/CPU)      |                                 |                             |
| プレビューの体感フレーム |                                 |                             |
| 撮影→切り抜き表示の時間  |                                 |                             |
| 全身(2〜3m)での抜け精度  |                                 |                             |
| 複数人が写ったときの精度 |                                 |                             |
| 輪郭の粗さ・欠け         |                                 |                             |

所見:

## 2. 家庭背景(リビング想定)での誤検出

- 誤検出しやすかった対象(家具・ぬいぐるみ・ポスター等):
- 誤検出の頻度:
- ノイズ処理(モルフォロジー + 面積閾値)導入後の改善度:
  ※ノイズ処理は `core/mask.ts` に実装予定。現時点の検証ページは未適用の生マスク

## 3. 複数人分離

- 2人が離れて立ったとき、別々の塊としてラベリングできたか:
- どの程度離れれば分離できるか(目安):
- 重なった場合の挙動:
  ※連結成分ラベリングは `core/mask.ts` に実装予定。現時点では未検証

## 4. 処理時間実測(目標 〜1秒)

| 端末 | モデル | 撮影→切り抜き表示 |
| ---- | ------ | ----------------- |
|      |        |                   |

## 5. 実機動作確認

| 端末 / OS / ブラウザ | カメラ起動 | マスク重畳 | キャプチャ | カメラ切替 | 備考 |
| -------------------- | ---------- | ---------- | ---------- | ---------- | ---- |
| iOS Safari           |            |            |            |            |      |
| Android Chrome       |            |            |            |            |      |
| PC Chrome            |            |            |            |            |      |

---

## 結論 / 次アクション

- 採用モデル:
- 理由:
- 実装時に対応が必要な点:
