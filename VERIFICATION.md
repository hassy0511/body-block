# 技術検証メモ

SPEC_MODE1.md §6 / CLAUDE.md §2.3 の検証結果を記録する。
検証ページ: `verify.html`(公開URL の `/body-block/verify.html`)

---

## 検証ページの使い方

1. 公開URL(HTTPS)または `npm run dev` のローカルURLを実機で開く
2. カメラ許可を出す → プレビューにマスクが水色で重畳表示される
3. 画面上部のセレクトでモデルを切り替える(切り替えのたびに再読み込みされる)
4. 「静止画キャプチャ」で切り抜き結果と処理時間がログに出る

ログには以下が出力される:

- モデル読み込み時間 / 使用デリゲート(GPU or CPU)
- キャプチャ解像度 / 人物ピクセルの割合 / 撮影→切り抜き表示までの処理時間

---

## 1. モデル比較(全身 2〜3m・複数人構図)

| 観点                     | Selfie Segmentation (landscape) | Multiclass Selfie Segmenter |
| ------------------------ | ------------------------------- | --------------------------- |
| モデル読み込み時間       |                                 |                             |
| デリゲート(GPU/CPU)      |                                 |                             |
| プレビューの体感フレーム |                                 |                             |
| 撮影→切り抜き表示の時間  |                                 |                             |
| 全身(2〜3m)での抜け精度  | 劣る                            | **良好**                    |
| 複数人が写ったときの精度 |                                 |                             |
| 輪郭の粗さ・欠け         |                                 |                             |

**所見: Multiclass を採用する。**

- 抜け精度が明確に上回る(iOS Safari で確認)
- 加えて、Multiclass はカテゴリ(髪 / 肌 / 顔 / 服 / その他)を区別できるため、
  「服だけの塊」を人物でないと判定できる。後述のジャケット誤検出対策は
  このモデルでしか実装できず、採用理由の2つ目になっている

## 2. 家庭背景(リビング想定)での誤検出

- 誤検出しやすかった対象: **ハンガーに掛けたジャケット等の衣類**
  - 両モデルとも人物の服を学習しているため、人が着ていなくても反応する
  - 面積が大きいので、単純な面積閾値では除去できない
- 対策として実装したもの(`core/mask.ts`):
  - モルフォロジー処理(オープニング → クロージング)で輪郭を平滑化し細かいノイズを除去
  - 面積閾値で小さいノイズ塊を除去
  - **人物の根拠判定**: 連結成分ごとに髪 / 肌 / 顔のピクセルを数え、
    これらを一定割合含まない塊は「人ではないもの」として捨てる。
    人が着ていれば必ず髪・肌・顔が写るが、ハンガーの衣類は「服」だけの塊になる
- 実機での改善度:
  ※検証ページの「ノイズ処理・かたまり判定を使う」チェックで ON/OFF を比較できる

## 3. 複数人分離

- 2人が離れて立ったとき、別々の塊としてラベリングできたか:
- どの程度離れれば分離できるか(目安):
- 重なった場合の挙動:
  ※8近傍の連結成分ラベリングを `core/mask.ts` に実装済み。
  検出した塊の数は検証ページのプレビュー枠とログに表示される

## 4. 処理時間実測(目標 〜1秒)

| 端末 | モデル | 撮影→切り抜き表示 |
| ---- | ------ | ----------------- |
|      |        |                   |

## 5. 実機動作確認

| 端末 / OS / ブラウザ | カメラ起動 | マスク重畳 | キャプチャ | カメラ切替 | 備考   |
| -------------------- | ---------- | ---------- | ---------- | ---------- | ------ |
| iOS Safari           | OK         | OK         | OK         | OK         |        |
| Android Chrome       |            |            |            |            | 未確認 |
| PC Chrome            |            |            |            |            | 未確認 |

---

## 結論 / 次アクション

- **採用モデル: Multiclass Selfie Segmenter**
- 理由:
  1. 全身 2〜3m の構図で抜け精度が Selfie Segmentation より明確に良い
  2. カテゴリを区別できるため、衣類のみの塊を人物から除外できる
- 実装時に対応が必要な点:
  - モデルサイズが約16MB あり初回読み込みが重い。
    本編では読み込み中の待機表示が必要
  - 未計測: 撮影→表示の処理時間、デリゲート(GPU/CPU)の実測値
  - 未確認: Android Chrome での動作、複数人が離れて立ったときの分離成功率

### 既知の課題

- 衣類の誤検出は「人物の根拠判定」で対処したが、
  人が着ている衣類の一部が分離した塊になった場合、それも除外されうる。
  実機での見え方次第でしきい値(`minEvidenceRatio`)の調整が必要
