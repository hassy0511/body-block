# モード5「いきものずかん」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
勝ち負けのない**低年齢向け鑑賞モード**。撮った体が生き物になって水槽を泳ぐ。

---

## 1. ゲーム概要

撮影するたび、その姿が「いきもの」になって画面いっぱいの水槽に加わり、
ふよふよと漂う。ぶつかると軽く押し合う。それだけ。

- ルール・制限時間・スコアは**一切ない**。眺めて楽しむ
- 何人で写ってもよい。写った人数ぶんの生き物が同時に生まれる
- 「なかまを よぶ」を押すたびに追加できる

## 2. 画面構成（縦向き基準）

```
┌─────────────┐
│┌カメラ┐         │
│└───┘         │
│なかま:○ひき      │
│  (水槽ぜんたい)   │
│ 〜 ふよふよ 〜    │
│  なかまを よぶ    │
│ きりかえ / やめる  │
└─────────────┘
```

- **水槽は画面全体**。カメラは左上に小さく浮かべる
- 生き物はカメラ表示より奥の深さに置き、カメラの下をくぐって泳ぐ

## 3. うごき

- 重力は効かせない（`setIgnoreGravity`）。水中に漂う表現
- 数秒ごとに小さなランダムの力で向きを変える
- 速さと回転には上限を掛け、ゆったりした動きに保つ
- 画面の四辺に見えない壁を置き、外へは出られない

## 4. 調整項目

`ZukanScene.ts` 冒頭の定数。

| 定数                  | 現在値 | 意味                                 |
| --------------------- | ------ | ------------------------------------ |
| `SHOT_TIME_LIMIT_SEC` | 5      | 撮影までの秒数（短めでよい）         |
| `MAX_CREATURES`       | 12     | 生き物の上限。超えたら古い順に消える |
| `MAX_HEIGHT`          | 150    | 生き物の大きさ上限(px)               |
| `SPEED_LIMIT`         | 2.4    | 泳ぐ速さの上限                       |

## 5. 実装上の注意

- 生き物の上限を必ず設ける。無制限だとテクスチャが増え続けて重くなる。
  消すときはスプライトとテクスチャの両方を破棄する
- 泳ぎの「ゆらぎ」は乱数の nudge（数秒おきの微小な速度変更）で作る。
  毎フレーム乱数を足すと痙攣して見える

## 6. スコープ外（今回は入れない）

- **選択制保存（CLAUDE.md §4）**: 「とっておく？」で localStorage に残す機能は
  このモードと相性がよいが、まずは遊びを検証してから別途着手する。
  デフォルト保存なしの方針はそのまま（現状は何も保存しない）
- 草原ステージなどの背景バリエーション
- 生き物どうしの相互作用（追いかける・群れる）
