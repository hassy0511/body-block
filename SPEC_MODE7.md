# モード7「あめあめキャッチ」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
順番に挑戦して数を競う。**「みんなでわいわい」路線の追加モード第2弾**。

---

## 1. ゲーム概要

うでで おわんの形を作るようなポーズで撮影すると、
体ブロックが舞台に置かれる。上からアメがたくさん降ってきて、
**体の上に乗ったアメの数**が点数。順番に挑戦して数を比べる。

- 「どんな形なら受け止められるか」の工夫が本体
  （タワーのポーズ戦略と同じ面白さの別アングル）
- ルールが一瞬で分かるので低年齢向き
- 1人なら自己ベスト狙い、2〜4人なら順番に挑戦して勝負

## 2. 画面構成とルール

タワーと同じ1画面構成・同じ落下マッピング（SPEC_MODE2.md §4）。

- 導入画面で「なんにんで きょうそうする？」（1〜4人）を選ぶ。
  1人ずつ順番に写るので、撮影ガイド枠は常に1人ぶん
- 1人の番 = 撮影1回 → ブロックが落ちて固定 → アメ 24 個が約 150ms 間隔で
  ランダムな x に降る → 落ち着いたら集計
- 集計: **床から十分高い位置(床-40px より上)で止まったアメ**を数える。
  床に転がったものは体で受け止められなかったもの
- 全員終わったら結果発表。最多が優勝、同数はひきわけ
- 1人プレイの結果コメント: 8個以上「すごい！」/ 4個以上「いいかんじ！」/
  それ未満「またちょうせん！」

### ブロックの固定

アメの重さや衝突で姿勢が崩れると「撮ったポーズで受け止めた」感が消えるので、
落ち着いた時点で `setStatic(true)` で固定する（モード6 と同じ理由）。

## 3. 調整項目

いずれも `CatchScene.ts` 冒頭の定数。

| 定数                | 現在値 | 意味                             |
| ------------------- | ------ | -------------------------------- |
| `STAGE_WIDTH`       | 560    | カメラ枠と舞台の幅(px)           |
| `CANDY_TOTAL`       | 24     | 1回に降るアメの数                |
| `CANDY_INTERVAL_MS` | 150    | アメの間隔(ms)                   |
| `CANDY_RADIUS`      | 13     | アメの半径(px)                   |
| `CAUGHT_Y`          | 床-40  | これより上で止まれば「キャッチ」 |

## 4. スコープ外

- アメの種類・点数差（金のアメ等）
- 同時対戦（舞台を分割して2人同時に降らせる）
- 保存・ランキング
