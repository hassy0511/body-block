# モード2「たいそうタワー」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
本モードは**物理演算の初実装**。切り抜きを剛体にして積み上げる。

---

## 1. ゲーム概要

好きなポーズで撮影し、切り抜かれた体の形をブロックとして落として積み上げる。
目標ラインまで崩さず積めたらクリア。

- 1人プレイ: 自分の形だけで積む
- 複数人プレイ: 全員の形が順番に落ちてくる
- ポーズ選びが戦略になる。大の字は積みにくく、丸まった形は積みやすい

## 2. 画面遷移

```
タイトル
  → モード選択（たいそうタワー）
  → あそびかた + 人数選択
  → 撮影画面（モード1 と共通。お題なしの自由ポーズ、制限時間つき）
  → タワー画面（タップで落下 → 積み上げ → 結果）
```

- 撮影画面は `CaptureScene` をモード1と共用する。
  お題の有無・期待するかたまり数・制限時間・案内文を
  `session.captureRequest` で渡し、撮影画面はモードを知らない

## 3. ブロック化（CLAUDE.md §2.2 の後半）

撮影後、かたまりごとに以下を行う（`core/cutout.ts`）。

```
かたまりのマスク
  → アルファ付きスプライト生成
      ※輪郭に白フチを付けてステッカー風にする
        （切り抜きの粗を隠す効果も兼ねる）
  → 輪郭抽出（ムーア近傍追跡）
  → 頂点間引き（Douglas-Peucker）
  → 凸分割（poly-decomp）
  → Matter.Bodies.fromVertices で剛体化
```

- **poly-decomp は必須**。これがないと Matter は凹んだ形を凸包に丸めてしまい、
  股や脇の隙間が埋まった「のっぺりした塊」になる。
  `core/physics.ts` の `registerDecomp()` で `window.decomp` に登録する
- 輪郭が複雑すぎて剛体化に失敗した場合は、矩形で代用して進行を止めない

## 4. ゲームルール

- ブロックは **8個**。撮影した形を順番に使い回す
- **タップした位置**から落ちる。落下位置は画面端から 80px 内側に丸める
- 落下時にわずかにランダムな角度をつける（毎回同じ積み方にならないように）
- 画面外へ落ちたブロックは取り除く
- **目標ライン**より上に、**崩れずに 1.5 秒**保てたらクリア

### 到達度の表示

- 到達した最高地点に印を引き、目標までの到達度を % で表示する
- ブロックを使い切って届かなかった場合も、**失敗で突き放さず**
  「ここまで つめたよ！ ゴールまで ○%」と到達度を見せる
  （子どもがもう一度やりたくなるようにする）

## 5. 調整項目

実機で遊びながら調整する。いずれも `TowerScene.ts` 冒頭の定数。

| 定数           | 現在値 | 意味                       |
| -------------- | ------ | -------------------------- |
| `TOTAL_BLOCKS` | 8      | 落とせるブロックの数       |
| `BLOCK_HEIGHT` | 180    | ブロックの高さ(px)         |
| `GOAL_Y`       | 470    | 目標ラインの高さ           |
| `HOLD_MS`      | 1500   | クリアに必要な静止時間(ms) |

- 積み上がる高さは**ポーズによって大きく変わる**。
  手足を広げた形は互いに引っかかって高さが出にくく、
  丸まった形は素直に積み上がる
- 開発中の計測では、大の字ポーズ 8 個で床から約 160〜220px

## 6. スコープ外（このモードではやらないこと）

- 保存機能（CLAUDE.md §4 の本格実装はモード5 と併せて）
- オンライン要素・ランキング
- ブロックの回転操作（落とす位置だけで遊ばせる）
