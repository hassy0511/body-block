# モード2「たいそうタワー」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
本モードは**物理演算の初実装**。切り抜きを剛体にして積み上げる。

---

## 1. ゲーム概要

好きなポーズで撮影し、切り抜かれた体の形をブロックとして落として積み上げる。
目標ラインまで崩さず積めたらクリア。

- 1人プレイ: 自分の形だけで積む
- 複数人プレイ: 全員の形が順番に落ちてくる
- ポーズ選びが戦略になる。大の字は積みにくく、丸まった形は積みやすい

## 2. 画面構成

```
タイトル
  → モード選択（たいそうタワー）
  → あそびかた + 人数選択
  → タワー画面（撮影と積み上げが同じ1画面で続く）
```

**撮ったその姿が、そのまま下へ落ちて積み上がる**のがこのモードの面白さなので、
撮影画面へ行き来させず、1画面で完結させる。

```
┌─────────────┐
│   ┌───────┐   │
│   │カメラ(+マスク)│   │ ← 画面上部
│   ├───────┤   │
│   │           │   │
│   │ 積み上げの舞台 │   │ ← 撮った姿がここへ落ちる
│   │           │   │
├───┴───────┴───┤
│ きりかえ/スタート！/やめる │
└─────────────┘
```

- **カメラ枠と舞台は同じ幅・同じ左右位置の「柱」にする**。
  画面いっぱいには広げない（後述のとおり幅がブロックの大きさを決めるため）

- カメラまわりは `game/cameraPanel.ts` に切り出し、モード1 とも共用する
- 落ちてくるブロックがカメラ表示に重ならないよう、カメラは手前の深さに置く
- 内カメラ/外カメラの切り替えボタンを置く。
  内カメラは自分の姿を見ながら立ち位置を決められ、
  外カメラは離れて全身を撮りやすい。
  ただし**カウントダウン中は切り替えさせない**（写る向きが変わると落下位置がずれるため）

## 3. ブロック化（CLAUDE.md §2.2 の後半）

撮影後、かたまりごとに以下を行う（`core/cutout.ts`）。

```
かたまりのマスク
  → アルファ付きスプライト生成
      ※輪郭に白フチを付けてステッカー風にする
        （切り抜きの粗を隠す効果も兼ねる）
  → 輪郭抽出（ムーア近傍追跡）
  → 頂点間引き（Douglas-Peucker）
  → 凸分割（poly-decomp）
  → Matter.Bodies.fromVertices で剛体化
```

- **poly-decomp は必須**。これがないと Matter は凹んだ形を凸包に丸めてしまい、
  股や脇の隙間が埋まった「のっぺりした塊」になる。
  `core/physics.ts` の `registerDecomp()` で `window.decomp` に登録する
- 輪郭が複雑すぎて剛体化に失敗した場合は、矩形で代用して進行を止めない

## 4. ゲームルール

- 撮影できるのは **6回**。撮るたびに、その姿が舞台の上から落ちてくる
- 複数人で写っていれば、その人数ぶんのブロックが同時に落ちる
- **切り抜きは「カメラに写っていたその位置・その大きさ」で現れて落ちる**。
  カメラの絵からそのまま剥がれ落ちるように見せる。
  - カメラ枠と舞台は**同じ幅**にする。これにより
    「カメラの左端に写った人は舞台の左端に落ちる」が成り立つ
  - 大きさもカメラ表示と同倍率。**一定サイズに正規化してはいけない**
    （写っていた姿と落ちてくる姿の大きさが違うと、
    どこに立てばどう落ちるのかが分からなくなる）
  - どこに立つか＝どこに落とすか、がこのゲームの唯一の操作なので、
    中央固定にしてはいけない
- 端末に近づくとブロックが大きくなりすぎるので、高さだけ
  `MAX_BLOCK_HEIGHT` で頭打ちにする。位置は頭打ち後もずらさない
- **立つ距離がそのまま難易度になる**。離れて撮れば小さいブロックで
  たくさん積む必要があり、近づけば大きいブロックで少なく済むが
  ポーズが見切れる
- 画面外へ落ちたブロックは取り除く
- **目標ライン（舞台の上端）**より上に、**崩れずに 1.2 秒**保てたらクリア

### 積み重なり方

- 新しく落ちてきた姿は、**下にある姿とすり抜けずに必ずぶつかる**
- 当たり判定は切り抜いた輪郭そのままなので、手足の隙間に別の姿が入り込む
- **先に置いたものほど重く、摩擦も強い**。
  土台が軽いと後から来たブロックに押されて崩れてしまうため

### 到達度の表示

- 到達した最高地点に印を引き、目標までの到達度を % で表示する
- ブロックを使い切って届かなかった場合も、**失敗で突き放さず**
  「ここまで つめたよ！ ゴールまで ○%」と到達度を見せる
  （子どもがもう一度やりたくなるようにする）

## 5. 調整項目

実機で遊びながら調整する。いずれも `TowerScene.ts` 冒頭の定数。

| 定数                  | 現在値      | 意味                          |
| --------------------- | ----------- | ----------------------------- |
| `TOTAL_SHOTS`         | 6           | 撮影できる回数                |
| `SHOT_TIME_LIMIT_SEC` | 8           | ポーズを作る制限時間(秒)      |
| `STAGE_WIDTH`         | 520         | カメラ枠と舞台の幅(px)        |
| `CAM_HEIGHT`          | 293         | カメラ枠の高さ(px)。幅の 9/16 |
| `MAX_BLOCK_HEIGHT`    | 260         | ブロックの高さの上限(px)      |
| `GOAL_Y`              | 舞台上端+40 | 目標ラインの高さ              |
| `HOLD_MS`             | 1200        | クリアに必要な静止時間(ms)    |

### ブロックの大きさは `STAGE_WIDTH` で決まる（重要）

落ちてくる大きさ ＝ **カメラ枠の幅 ÷ 映像の幅**。
したがって**ブロックを小さくする方法は枠を細くすることだけ**で、
枠の高さを変えても大きさはほとんど変わらない（写る範囲が変わるだけ）。

- 画面いっぱいの 720px だったときは、そこそこ離れて撮っても
  ブロックが舞台の半分を占め、2 個で目標に届いてしまっていた
- 520px にすると、離れて立った人（映像の高さの 7 割）で
  ブロック約 220px。積む高さ 661px に対して 6 個で 6 割強まで届く
- `CAM_HEIGHT` は幅の 9/16 にしてある。
  **横長の映像をほぼ切り取らずに写せる比率**なので全身が入りやすい。
  横長すぎる枠にすると映像が上下に切り取られる
  （720x330 の頃は帯のようになり、顔しか写らなかった）

- 積み上がる高さは**ポーズによって大きく変わる**。
  手足を広げた形は互いに引っかかって高さが出にくく、
  丸まった形は素直に積み上がる
- 開発中の計測（縦長のポーズ 6 個）では到達度 6 割強。
  きれいに積めばもう少し伸びる、くらいの手応えを狙っている

### 実装上の注意（つまずいた点）

- **輪郭を Matter へ渡すときに拡縮を掛けてはいけない。**
  Phaser の `setScale` は剛体も一緒に拡縮するため、
  頂点側でも掛けると二重に縮み、見た目 150px の人型に対して
  当たり判定が 12x37px しかない状態になる（すり抜けたように積み上がる）
- **絵の原点を輪郭の重心に合わせる。**
  Matter は頂点列の重心を剛体の原点にするため、
  合わせないと絵と当たり判定がずれる
- **落下位置と倍率は、カメラのプレビュー表示から逆算する。**
  推論は「鏡像・切り出しずみのプレビュー画像」に対して行っているので、
  静止画の 1px が画面上で何 px だったか（＝`STAGE_WIDTH / 静止画の幅`）を
  そのまま倍率にすれば、位置も大きさもプレビューと一致する。
  そのために `CutoutPiece` に `origin`（スプライト左上が静止画のどこか）を持たせている

## 6. スコープ外（このモードではやらないこと）

- 保存機能（CLAUDE.md §4 の本格実装はモード5 と併せて）
- オンライン要素・ランキング
- ブロックの回転操作（落とす位置だけで遊ばせる）
