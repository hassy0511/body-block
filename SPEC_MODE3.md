# モード3「まもって！シェルター」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
モード2 の物理基盤（`cameraPanel` / `cutout` / `physics`）をそのまま流用する。

---

## 1. ゲーム概要

床にいる小さな生きもの（たまごちゃん）に、上からボールが降ってくる。
自分の体を撮って屋根にし、当たらないように守りきる。

- 1人プレイ: 自分の形だけで屋根を作る
- 複数人プレイ: 全員の形が同時に落ちてくるので一度に広く覆える
- **ポーズ選びが戦略になる**。大の字は広く覆えるが隙間だらけ、
  丸まった形は狭いが隙間がない

モード2 との違いは「**高く積む**」ではなく「**広く覆う**」こと。
同じ物理でも狙いが変わるので、遊び味が変わる。

## 2. 画面構成

```
┌─────────────┐
│のこり     ♥♥♥ │
│  ┌────────┐ │
│  │カメラ(+マスク)│ │
│  ├────────┤ │
│  │  ボール↓    │ │
│  │  屋根       │ │
│  │  たまご      │ │
├──┴────────┴─┤
│ きりかえ/スタート！/やめる │
└─────────────┘
```

- モード2 と同じく、**カメラ枠と舞台は同じ幅・同じ左右位置の柱**にする
  （「カメラの左端に写った人は舞台の左端に落ちる」を保つため）
- 舞台はモード2 より**広い**（560px）。高く積む必要がなく、
  横に広く覆えることのほうが大事なため

## 3. 進行

```
まもりタイム（撮影 4回）
  → ボールタイム（12秒間ボールが降る）
  → けっか（のこったハート）
```

- **撮影と防御を分けて 2 段階**にする。
  降っている最中に撮らせると、ポーズを作りに行っている間に
  何もできず当たるだけになってしまう
- ボールタイムに入ったら撮影はできない

## 4. ゲームルール

- 撮影できるのは **4回**。撮った姿はモード2 と同様、
  **カメラに写っていたその位置・その大きさ**で落ちてくる
- 落ちた体は積み上がって屋根になる
- たまごちゃんは床の上のランダムな位置にいる。
  **どこにいるかを見てから立ち位置を決める**のがこのモードの考えどころ
- ボールタイムでは一定間隔でボールが降る
- **ボールがたまごちゃんに当たるとハートが1つ減る**。3つ減るとおしまい
- ボールタイムを守りきればクリア

### 守りきれなくても突き放さない

- 失敗しても「ぜんぶ まもれなかった」ではなく
  **何個防いだか**を見せる（CLAUDE.md の子ども向け方針）

## 5. 調整項目

`ShelterScene.ts` 冒頭の定数。

| 定数                  | 現在値 | 意味                     |
| --------------------- | ------ | ------------------------ |
| `TOTAL_SHOTS`         | 4      | 撮影できる回数           |
| `SHOT_TIME_LIMIT_SEC` | 8      | ポーズを作る制限時間(秒) |
| `STAGE_WIDTH`         | 560    | カメラ枠と舞台の幅(px)   |
| `HEARTS`              | 3      | 当たってよい回数         |
| `WAVE_MS`             | 12000  | ボールが降る時間(ms)     |
| `BALL_INTERVAL_MS`    | 620    | ボールの間隔(ms)         |
| `BALL_RADIUS`         | 16     | ボールの大きさ(px)       |

- `STAGE_WIDTH` はモード2 より広い 560px。
  ブロックの大きさは**枠の幅で決まる**（SPEC_MODE2.md §5 参照）ので、
  広くすると体が大きく写り、広く覆えるようになる。
  このモードでは大きいほうが都合がよい

## 6. 実装上の注意

- たまごちゃんは**物理ボディにしない**。
  剛体にするとボールが跳ね返って何度も当たり判定が起きるうえ、
  落ちてきた体に押しつぶされて動いてしまう。
  絵として置き、ボールとの距離だけ見て当たりを判定する
- ボールは画面外へ出たら取り除く（残すと際限なく増える）

## 7. スコープ外

- 保存機能（モード5 と併せて）
- ボールの種類・特殊効果
- たまごちゃんの移動（止まっているほうが守る場所が決めやすい）
