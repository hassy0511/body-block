# モード4「こわしてバトル」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
モード2〜3 の物理基盤（`cameraPanel` / `cutout` / `blockFactory`）を流用する。

---

## 1. ゲーム概要

積み木のタワーのてっぺんに かんむり が乗っている。
自分の体を撮って落とし、タワーを崩して かんむり を落とした方が勝ち。

- **ふたりで**: 左右にタワーが1本ずつ。交互に撮影し、
  相手のタワーへ自分の体が落ちる。先に相手の かんむり を落とした方の勝ち
- **ひとりで**: タワーが1本。5回の撮影で崩せたらクリア
- **立ち位置がそのまま狙いになる**。カメラの左に写ればタワーの左に落ちる。
  端を狙って崩すか、真上から押しつぶすかの選択がある

## 2. 画面構成（縦向き基準）

```
┌─────────────┐
│ばん/のこり        │
│    ┌カメラ┐      │
│    └───┘      │
│      ▼(ねらい)   │
│ ┌タワー┐ ┌タワー┐ │
│ │あか  │ │あお  │ │
│    (あんない)     │
│ きりかえ/スタート/やめる│
└─────────────┘
```

- カメラは中央上。**カメラ枠の幅 = タワーの柱の幅**にし、
  写った位置を攻撃先の柱の同じ位置に対応させる（モード2 の原則の応用）
- ふたり: 柱は左右に1本ずつ。あか(P1)の陣地が左、あお(P2)が右
- ひとり: 柱はカメラの真下に1本
- 攻撃先の柱の上に▼マークを出し、どこへ落ちるかが分かるようにする

## 3. 進行

- ふたり: あか → あお → あか… と交互。自分の番に「スタート！」→ 8秒で
  ポーズ → 撮った姿が**相手の**タワーへ落ちる
- 落ちた後、全体が落ち着く（または4秒経過）まで待ってから次の番へ
- かんむりが床の高さまで落ちたら、その持ち主の負け
- 1人5回ずつ使い切って両方立っていたら**ひきわけ**

## 4. 調整項目

`BattleScene.ts` 冒頭の定数。

| 定数                  | 現在値 | 意味                         |
| --------------------- | ------ | ---------------------------- |
| `MAX_SHOTS_EACH`      | 5      | 1人あたりの撮影回数          |
| `SHOT_TIME_LIMIT_SEC` | 8      | ポーズを作る制限時間(秒)     |
| `COL_WIDTH`           | 300    | 柱とカメラ枠の幅(px)         |
| `TOWER_BLOCKS`        | 4      | タワーの段数                 |
| `MAX_BLOCK_HEIGHT`    | 170    | 落とすブロックの高さ上限(px) |
| `SETTLE_TIMEOUT_MS`   | 4000   | 落ち着き待ちの上限(ms)       |

- 落とすブロックの密度はタワーの積み木より重くしてある。
  軽いと乗るだけで崩れず、決着がつかない

## 5. 実装上の注意

- **かんむりの転落は定位置からの相対で判定する**。
  「床まで落ちたか」では、がれきの山の上に乗ると成立しないことがあった。
  定位置から2段ぶん下がる、または柱の外へ弾かれたら転落。
  一瞬のバウンドで誤判定しないよう 500ms 持続でのみ成立させる
- 撮影に失敗した回（誰も写っていない等）は回数を消費しない
- 剛体化は `blockFactory.spawnCutoutBlock()` を使う（モード2 の教訓ずみ）

## 6. スコープ外

- 3人以上の対戦（トーナメントは将来）
- タワーの形バリエーション
- 攻撃ブロックの回転操作
