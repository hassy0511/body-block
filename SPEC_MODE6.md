# モード6「コロコロゴール！」詳細仕様

親ドキュメント: `CLAUDE.md`（プロジェクト全体仕様）
協力パズル。**「みんなでわいわい」路線の追加モード第1弾**。

---

## 1. ゲーム概要

上のシューターからボールが転がり落ちてくる。
みんなで交代にポーズを撮り、体ブロックで「みち」を作って、
床のゴールかごまでボールを導けたらクリア。

- 撮影は 6 回まで。1回撮るごとにボールが転がって試してくれる
- 1人でも遊べるが、「あと1個どこに置く？」を相談しながら遊ぶ協力型が本体
- ポーズが道具になる。横に寝そべれば坂道、丸まればストッパー

## 2. 画面構成

タワー(モード2)と同じ1画面構成。カメラ枠と舞台は同じ幅・同じ左右位置で、
「写った位置・写った大きさで落ちる」の原則もそのまま（SPEC_MODE2.md §4）。

```
┌─────────────┐
│   ┌───────┐   │
│   │カメラ(+マスク)│   │
│   ├───────┤   │
│   │▼ボールの出口   │   │ ← 舞台上端。左右どちらかに毎回ランダム
│   │           │   │
│   │  ゴールかご    │   │ ← 床の上。出口と反対側にランダム
├───┴───────┴───┤
│ きりかえ/スタート！/やめる │
└─────────────┘
```

## 3. ゲームルール

- 撮影 6 回。撮るたびに:
  1. 写った姿がその位置・その大きさで落ちる（複数人写れば全員ぶん）
  2. ブロックが落ち着いたら**固定する**（後述）
  3. 出口からボールが 1 個転がり出る
  4. かごに入ればクリア。止まったり時間切れになったら次の撮影へ
- 6 回使い切って届かなければ「とどかなかった…」（すぐ再挑戦できる）
- クリア時は使ったブロック数を表示する（少ないほどすごい、は言外に）

### ブロックの固定（このモードの肝）

**落ち着いたブロックは `setStatic(true)` で固定する。**
固定しないと、次のブロックやボールに押されて道が崩れ、
「どこに置くか」を考える遊びにならない。
タワーは「崩れるかも」がスリルだが、このモードは置いた結果が
残ることが安心材料になる。

### ゴールかご

- 壁の高さ(96px)をボールの直径より十分高くして、
  **床を転がってきただけでは入らない**ようにする。
  上から落とさないと入らない = みち作りが必要になる
- 入った判定は「かごの内側 x 範囲」かつ「壁の上端より下」の位置判定

### ボールのあきらめ判定

- 速度 0.2 未満が 1.4 秒続いたら「止まった」
- 出てから 10 秒たったら打ち切り（どこかで転がり続けても終わる）

## 4. 調整項目

いずれも `RollScene.ts` 冒頭の定数。

| 定数            | 現在値 | 意味                      |
| --------------- | ------ | ------------------------- |
| `TOTAL_SHOTS`   | 6      | 撮影できる回数            |
| `STAGE_WIDTH`   | 560    | カメラ枠と舞台の幅(px)    |
| `BALL_RADIUS`   | 18     | ボールの半径(px)          |
| `BASKET_HALF`   | 52     | かごの内側の半分の幅(px)  |
| `BASKET_HEIGHT` | 96     | かごの壁の高さ(px)        |
| `BALL_STUCK_MS` | 1400   | 止まったとみなす時間(ms)  |
| `BALL_MAX_MS`   | 10000  | 1球ごとの持ち時間上限(ms) |

## 5. スコープ外

- ボールの複数個・同時転がし
- ステージのバリエーション（出口とかごの位置ランダムで代える）
- ブロックの削除・置き直し操作
