# 2台接続（カメラやく / 画面やく）

親ドキュメント: `CLAUDE.md`
実験ページ: `pair.html`（`src/pair/`）。**ゲーム本体にも組み込みずみ**（§6）。

---

## 1. ねらい

全身を写すには端末から 1.5〜3m 離れる必要があり、1台では画面が読めない
（CLAUDE.md §6「遊ぶ環境の前提」）。そこで:

- **カメラやく**: 部屋の向こうに立てて、写して送るだけ
- **画面やく**: 手元に置き、ゲーム本体を動かす

の2台構成にする。距離の問題が根本から消える。

## 2. 通信方式と §2.1 の整合

- 映像は **WebRTC の P2P** で端末から端末へ直接送る。
  **STUN も指定しない**。同じWi-Fi内なら host 候補だけでつながり、
  外部サーバーへの問い合わせが一切発生しない
- 接続確立に必要な情報(SDP)の交換もサーバーを使わず、
  **QRコードの見せ合い**で行う（手動コピーの代替手段つき）
  1. 画面やくがオファーQRを表示
  2. カメラやくが読み取り、アンサーQRを表示
  3. 画面やくが読み返して接続完了
- SDP は deflate 圧縮 + base64url で QR に収める（`src/pair/signalCode.ts`。
  接頭辞 `KBQR1:`）。実測でオファー約1.4KB → QR 1枚に収まる
- ICE は Vanilla 方式（候補が出そろってから1枚のQRにまとめる）。
  出そろわないルーターがあるため3秒で打ち切る

## 3. 使用ライブラリ（クレジット画面に記載すること）

| ライブラリ | 用途     | ライセンス |
| ---------- | -------- | ---------- |
| qrcode     | QR生成   | MIT        |
| jsQR       | QR読取り | Apache-2.0 |

## 4. 既知の制約

- **同じWi-Fi必須**。ルーターの AP 分離（プライバシーセパレーター）が
  有効だと端末同士が通信できず失敗する。エラー文言で案内している
- iOS 16.4 以降の Safari が必要（CompressionStream）。
  非対応ブラウザには入口で案内を出す
- カメラやくは画面が消えると配信が止まる。Wake Lock API を試みるが、
  非対応端末では「自動ロックを切る」案内が必要（未実装）

## 5. 検証項目（実機2台で）

- [x] iPhone × iPad で QR 握手 → 接続 → 映像が届く（実機で確認ずみ）
- [ ] 経路が `host (同一LAN直結)` になっている（統計表示で確認できる）
- [ ] 2〜3m 離れた全身が画面やく側で見える画質か
- [ ] 5分放置して映像が止まらないか（スリープ・発熱）
- [ ] 遅延の体感（ポーズ→画面反映まで）

## 6. ゲーム本体への組み込み（実装ずみ）

**2台とも同じゲームを開き、タイトルの「2だいで つなぐ」から
「がめんやく / カメラやく」を選ぶ**。別ページを開かせない
（役ごとに開くものが違う設計はユーザーに優しくないため変更した）。
`pair.html` は接続診断(統計表示つき)の開発用ページとして残す。

- タイトル下部の「2だいで つなぐ」→ 役選択つきQR握手のオーバーレイ
  （`game/pairOverlay.ts`。QRまわりはDOMが楽なので Phaser シーンにしない）
- カメラやく を選んだ端末は、接続後「そうしんちゅう」画面のまま
  立てて置く。「そうしんを やめる」で終了
- つながったら `core/remoteCamera.ts` にストリームを預ける。
  接続は**シーンをまたいで生きる**ので、全モードをそのまま2台で遊べる
- `CameraController.start()` はリモートがあればそれを使う。
  **鏡像で表示する**。一度「テレビ中継と同じ向きが自然」と鏡像をやめたが、
  プレイヤーは画面を鏡として立ち位置を直すので、鏡像でないと
  左右の調整が逆になり遊びにくかった(実機で両方試した結論)
- `stop()` はリモートのときトラックを止めず外すだけ。
  止めると次のシーンで接続が死ぬ
- 切断(相手が閉じた等)は自動で解除され、次から手元のカメラに戻る。
  タイトルの表示「📡 2だいモードで あそびちゅう」から手動で切ることもできる
- 「カメラきりかえ」ボタンは2台モード中は何もしない

### 実装上の注意（つまずいた点）

- **カメラの起動は役を選んだ直後に1回だけ行い、最後まで使い回す**。
  iOS は2回目以降のカメラ起動(手順の途中での起動し直し)で固まりやすい。
  がめんやくは QR 表示中から裏で起動して保持し、②で見せるだけにする。
  カメラやくは QR 読み取りに使ったカメラをそのまま送信に使う
- **カメラやく端末は縦に立てると縦長の映像を送ってくる**。
  16:9 のカメラ枠へ cover で入れると上下が大きく捨てられ、
  視野が細い帯になってしまった(実機で発生)。枠と映像の比率が
  1.5倍以上ずれるときは contain(全体を収めて左右に余白)で描く。
  撮影も同じ構図のキャンバスに描くので、見えている画と判定が一致する。
  余白のぶん舞台の端には落とせない。舞台幅をぜんぶ使いたければ
  カメラやく端末を横置きにする
- **`hidden` 属性は CSS の `display` 指定に負ける**。
  `display: grid` を当てた要素は `hidden` にしても消えない。
  オーバーレイの CSS 先頭に `[hidden] { display: none !important; }` を置いた
- **読み取りカメラの起動待ちと手動コード入力が競合する**。
  `getUserMedia` の解決前にコードが入ると、あとから読み取りビューが
  起動して残ってしまう。トークンで世代管理して、待っている間に
  止められたら捨てる
