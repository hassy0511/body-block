# モード1「ポーズでハマる！」詳細仕様

親ドキュメント: `SPEC.md`（プロジェクト全体仕様）
本モードは**切り抜き精度の実機検証を兼ねた最初の一本**。物理演算は使わない。

---

## 1. ゲーム概要

画面に表示されるシルエットの「穴」と同じ形になるようポーズをして撮影し、
切り抜きマスクと穴の一致度をスコア化するゲーム。

- 1人プレイ: 自分ひとりで穴の形に挑戦
- 複数人プレイ: 全員で協力して 1 つの大きな穴の形を作る（同時撮影）
- 1 ゲーム = 3 つのお題（穴）に連続挑戦し、合計スコアで結果発表

## 2. 画面遷移

```
タイトル
  → 人数選択（1〜3人）
  → あそびかた説明（初回のみ / スキップ可）
  → お題表示（穴のシルエットを数秒見せる）
  → 撮影画面（プレビュー + マスクリアルタイム表示 + ガイド + カウントダウン）
  → 判定演出（切り抜きが穴に飛んでいき、ハマる演出 → スコア表示）
  → （×3 お題繰り返し）
  → 結果画面（合計スコア / ランク / もういちど / タイトルへ）
```

### 撮り直しフロー

- 撮影後、判定前に切り抜き結果のプレビューを一瞬表示
- 検出塊数がお題の `expectedBlobs`（§4 参照）と合わない場合:
  「はなれて たちなおしてね！」と表示し撮影画面へ戻る（スコア計上しない）
  ※プレイヤー数ではなくお題側の想定塊数と比較する。
  手をつなぐお題では 2 人でも 1 塊が正解になるため
- 塊数が合っていれば自動で判定へ進む（テンポ重視。手動の撮り直しボタンも
  撮影画面に常設する）

### 判定後の撮り直し（子ども向けとして重要）

- 判定画面には **「もういちど とる」と「つぎの おだいへ」を必ず並べる**。
  低い点だと「もういっかい！」と出るのに撮り直せず次へ進むしかない、
  という状態にしてはいけない。言われたとおりにできないと子どもは納得しない
- **回数制限は設けない**。何度でも同じお題に挑戦できる
- 記録に残すのは **いちばん良かった回**。
  撮り直して悪くなったぶんが残ると、挑戦するほど損になり撮り直さなくなる
- 2 回目以降は「じこベスト こうしん！」か「きろくは ○てん のまま」を添えて、
  何が記録されたのかが分かるようにする

## 3. 判定ロジック（マスク重なり率）

物理演算を使わず、二値マスク同士の比較のみで判定する。

```
入力:
  P = プレイヤーの人物マスク（全員分を合算した二値マスク、縮小解像度で可）
  T = お題の穴マスク（同解像度の二値マスク）

前処理:
  P の重心と T の重心を合わせる（位置ズレの吸収）
  P を T のバウンディングボックスにフィットするよう等倍スケール調整
  ※回転補正はしない（ポーズで合わせるのがゲームなので）

スコア計算（IoU ベース）:
  overlap  = |P ∩ T|            … 穴に入っている部分
  miss     = |T \ P|            … 穴なのに埋まっていない部分
  overflow = |P \ T|            … 穴からはみ出した部分

  score = 100 × |P ∩ T| / |P ∪ T|   （= IoU × 100）
```

- 表示は 0〜100 の「ハマり度」。演出上は 5 点刻み程度に丸めて甘めに見せてよい
- ランク例: 90+ = 「かんぺき！」 / 70+ = 「いいかんじ！」 / 50+ = 「おしい！」 /
  それ未満 = 「もういっかい！」
- 判定演出では miss 部分と overflow 部分を色分け表示して
  「どこが合ってなかったか」を視覚的にフィードバックする

## 4. お題（穴）のデータ仕様

- お題はグレースケール PNG（白 = 穴、黒 = 壁）+ メタ情報で管理する。
  画像は `public/themes/` に置き、メタ情報は `src/themes/themes.ts` に持つ

```ts
{
  id: 'daimonji',
  name: 'だいのじ',
  players: 1,
  difficulty: 1,          // 1〜3
  file: 'daimonji.png',
}
```

- 画像は 1280 x 720 の 8bit グレースケール。中間色を含まない完全な二値であること
- 読み込み時に幅 256px へ縮小して二値化し、判定用マスクとする
  （プレイヤー側のノイズ処理後の解像度と揃えている）
- お題一覧は `themes.html` で目視確認できる（元画像と判定用マスクを並べて表示。開発用）

### かたまりの数（`expectedBlobs`）

- 読み込み時にお題マスクの連結成分を数え、`expectedBlobs` として保持する
- **これは人数と一致するとは限らない。** 手をつなぐお題は 2 人でも 1 つのかたまりになる
- 撮り直し判定（§2）はプレイヤー数ではなく **この値** と比較する

- 初期セット: 1人用 × 6 種、2人用 × 4 種、3人用 × 2 種 程度
  - 1人用の例: 大の字 / 片足バランス / しゃがみ玉 / バンザイ / くの字 / T ポーズ
  - 2人用の例: ハート（2人で半分ずつ）/ 山と谷 / 大小コンビ / トンネル
- お題シルエットは**全て自作**（人型テンプレートから作成）。
  既存キャラクターや番組演出を連想させる形状は作らない（SPEC.md §5 参照）
- お題選択はランダム（同一ゲーム内で重複しない）

## 5. 撮影画面の要件（共通基盤の初実装を兼ねる)

### お題の見せ方（このモードが成立するかどうかの要）

**穴が見えていないとポーズの取りようがなく、ゲームとして成立しない。**
以下は「あったほうがよい」ではなく必須要件として扱う。

- カメラプレビュー上に、お題を**「型」として重ねる**。
  - **穴の中は透明**にして自分の姿がそのまま見えるようにする
  - **穴の外(壁)を暗く覆う**
  - 境目には**太い明るいふち**を描く
  - 半透明のベタ塗りを重ねるだけでは、カメラ映像に埋もれて見えない
- **重ね表示はカメラ映像より手前の深さに置く**。
  奥に置くと映像に完全に隠れ、まったく見えなくなる
- **カメラ枠の縦横比をお題画像に合わせる**（お題は 16:9 なので枠も 16:9）。
  比率が違うと、重ねて見せている形と判定に使う形が食い違い、
  「見た目は合っているのに点が出ない」ことになる
- お題全体の形は穴に重ねただけだと掴みにくいので、
  **手本のシルエットを別に小さく表示**する
- **いまのハマり度をリアルタイムに表示**する。
  プレビューの推論結果を本判定と同じ `judgePose()` にかけるので、
  表示される点数と撮影後の点数が食い違わない。
  合わせている最中に見るものなので、カメラのすぐ下に大きく置く
- セグメンテーションマスクをリアルタイムで薄く色付け表示
  （抜け具合の事前確認 = SPEC.md §2.4）
- プレビュー中のセグメンテーションは低解像度・低頻度（例: 5〜10fps）でよい。
  本判定は撮影した静止画 1 枚に対してフル解像度で行う
- **制限時間制**。「スタート！」を押すと 10 秒のカウントダウンが始まり、
  0 になった瞬間に自動でシャッターが切られる
  - 端末から離れて構えるため、押してすぐ撮るのではなく
    **「制限時間内にポーズを作りきる」ことが面白さの中心**になる
  - 残り時間は画面中央に大きく表示する（プレイヤーは離れた位置から見るため）
  - 残り 3 秒を切ったら赤字＋高い音にして切迫感を出す
- カウントダウン音・シャッター音は WebAudio で合成する（自作。§SPEC.md 5 のライセンス制約を回避）
- インカメラ / アウトカメラ切り替えボタン（家庭ではタブレットを立てて
  インカメラで遊ぶケースを主想定。プレビューは鏡像表示にする）

## 6. 技術検証チェックリスト(このモードの開発中に記録すること)

SPEC.md §2.3 の検証をこのモード開発に組み込む。以下を README または
検証メモに記録する:

- [x] モデル比較: 全身 2〜3m・複数人構図での Selfie Segmentation vs
      Multiclass / DeepLab 系の精度・速度 → **Multiclass 採用**
- [x] 家庭背景(リビング想定)での誤検出頻度とノイズ処理後の改善度
      → 衣類・椅子の誤検出を「髪・肌・顔を含まない塊は捨てる」判定で解消
- [ ] 2人が離れて立ったときのラベリング分離成功率
- [x] 撮影→判定表示までの処理時間実測(目標 〜1秒) → **88〜99ms**
- [ ] iOS Safari / Android Chrome 双方での動作確認 → iOS Safari のみ確認済み

## 7. スコープ外(このモードではやらないこと)

- 物理演算・剛体化(モード2 以降で共通基盤に追加)
- 切り抜きの保存 UI(共通基盤の storage.ts は空実装 or 最小で可。
  本格実装はモード5 と併せて)
- オンライン要素・ランキング・SNS シェア(全て将来検討)
